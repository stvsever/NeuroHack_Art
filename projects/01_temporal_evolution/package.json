{
  "name": "eight-forms-of-intelligence",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "node build_offline.mjs && vite --host 127.0.0.1",
    "build": "tsc --noEmit && node build_offline.mjs",
    "build:vite": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.1",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3",
    "vite": "^7.0.5"
  }
}
